#include <iostream>

using namespace std;

/* int main() {

	double a, b, c, d, e, v;

	cin >> a;
	cin >> b;
	cin >> c;
	cin >> d;
	cin >> e;

	v = (5 / ((1 / a) + (1 / b) + (1 / c) + (1 / d) + (1 / e)));
	cout << v;
	return 0;
}*/